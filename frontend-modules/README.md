# frontend-modules

Pending. Will hold the extracted Angular `hrm` module (currently `frontend/src/app/modules/hrm` in
`ST000065_MECHELLIN_2.0`), plus the HRM-owned slice of `modules/masters` (employee, department,
designation, grade, leave-*, shift-*, holiday-master, allowance-*, salary-components, ticket-master),
mirroring the `backend-apps` split.
